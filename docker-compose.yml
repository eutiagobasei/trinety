services:
  postgres:
    image: postgres:16-alpine
    container_name: trinety-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: trinety
      POSTGRES_PASSWORD: trinety123
      POSTGRES_DB: trinety
    ports:
      - "5450:5432"
    volumes:
      - trinety_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U trinety"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - trinety-network

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: trinety-api
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      - NODE_ENV=development
      - PORT=3000
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_USERNAME=trinety
      - DB_PASSWORD=trinety123
      - DB_DATABASE=trinety
      - JWT_SECRET=trinety-dev-jwt-secret-key-2024-local
      - JWT_EXPIRES_IN=7d
      - JWT_REFRESH_EXPIRES_IN=30d
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
      - FRONTEND_URL=http://localhost:8082
      - THROTTLE_TTL=60
      - THROTTLE_LIMIT=100
    ports:
      - "3060:3000"
    volumes:
      - ./backend/src:/app/src:delegated
    networks:
      - trinety-network

  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend
    container_name: trinety-web
    restart: unless-stopped
    depends_on:
      - backend
    environment:
      - VITE_API_URL=http://localhost:3060/api
    ports:
      - "5190:8080"
    volumes:
      - ./src:/app/src:delegated
      - ./public:/app/public:delegated
      - /app/node_modules
    networks:
      - trinety-network

volumes:
  trinety_postgres_data:

networks:
  trinety-network:
    driver: bridge
